import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  container: {
    width: 70,
    height: 70,
    backgroundColor: '#9ab4ff',
    borderRadius: 75,
    position: 'absolute',
  }
})